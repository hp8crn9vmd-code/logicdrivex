---
import SiteLayout from "../layouts/SiteLayout.astro";
import HeroBackground from "../components/HeroBackground.astro";
import { offers } from "../data/offers";

const pageTitle = "LogicDriveX — Landing Offers";
const base = import.meta.env.BASE_URL;

const year = new Date().getFullYear();
---
<SiteLayout title={pageTitle} lang="en">
  <main class="min-h-screen bg-slate-50">
    <div class="mx-auto max-w-6xl px-4 py-10 sm:px-6 lg:px-8">

      <!-- Top bar -->
      <header class="flex items-center justify-between gap-4">
        <div class="flex items-center gap-3">
          <div class="h-10 w-10 rounded-2xl bg-slate-900"></div>
          <div>
            <p class="text-sm font-semibold text-slate-900">LogicDriveX</p>
            <p class="text-xs text-slate-600">Astro + Tailwind + TypeScript</p>
          </div>
        </div>

        <nav class="flex items-center gap-3">
          <a
            href={`${base}request-quote`}
            class="rounded-xl border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-900 hover:bg-slate-100"
          >
            Request a quote
          </a>
        </nav>
      </header>

      <!-- Hero -->
      <section class="relative mt-10 rounded-3xl border border-slate-200 bg-white p-8 sm:p-10">
        <HeroBackground />
        <div class="grid gap-10 lg:grid-cols-2 lg:items-center">
          <div>
            <h1 class="text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl">
              Professional landing system with clear scope
            </h1>
            <p class="mt-4 text-slate-700">
              Choose one of three offers. Each offer has a dedicated landing page and a real request-quote flow.
              This site avoids fake claims (no testimonials, no fabricated metrics). Everything is scope-based and explicit.
            </p>

            <div class="mt-6 flex flex-wrap gap-3">
              <a
                href="#offers"
                class="rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white hover:bg-slate-800"
              >
                View offers
              </a>
              <a
                href={`${base}request-quote`}
                class="rounded-xl border border-slate-300 bg-white px-5 py-3 text-sm font-semibold text-slate-900 hover:bg-slate-100"
              >
                Request a quote
              </a>
            </div>

            <div class="mt-6 grid gap-3 sm:grid-cols-3">
              <div class="rounded-2xl border border-slate-200 bg-slate-50 p-4">
                <p class="text-xs font-semibold text-slate-600">Delivery</p>
                <p class="mt-1 text-sm font-semibold text-slate-900">Static build</p>
                <p class="mt-1 text-xs text-slate-600">Fast pages, no backend required.</p>
              </div>
              <div class="rounded-2xl border border-slate-200 bg-slate-50 p-4">
                <p class="text-xs font-semibold text-slate-600">Form</p>
                <p class="mt-1 text-sm font-semibold text-slate-900">Web3Forms</p>
                <p class="mt-1 text-xs text-slate-600">Real submission flow.</p>
              </div>
              <div class="rounded-2xl border border-slate-200 bg-slate-50 p-4">
                <p class="text-xs font-semibold text-slate-600">Scope</p>
                <p class="mt-1 text-sm font-semibold text-slate-900">Explicit</p>
                <p class="mt-1 text-xs text-slate-600">Includes / excludes listed.</p>
              </div>
            </div>
          </div>

          <!-- Visual block (no fake screenshots) -->
          <div class="rounded-3xl border border-slate-200 bg-gradient-to-b from-white to-slate-50 p-6">
            <div class="rounded-2xl border border-slate-200 bg-white p-6">
              <p class="text-xs font-semibold text-slate-600">What you get</p>
              <h2 class="mt-2 text-lg font-bold text-slate-900">A clean landing setup</h2>
              <ul class="mt-4 space-y-3 text-sm text-slate-700">
                <li class="flex gap-3">
                  <span class="mt-1 h-2 w-2 rounded-full bg-slate-900"></span>
                  Dedicated landing pages per offer (no dynamic slug reuse).
                </li>
                <li class="flex gap-3">
                  <span class="mt-1 h-2 w-2 rounded-full bg-slate-900"></span>
                  Request-quote page with offer selection + customization details.
                </li>
                <li class="flex gap-3">
                  <span class="mt-1 h-2 w-2 rounded-full bg-slate-900"></span>
                  Thank-you page after successful submission.
                </li>
              </ul>

              <div class="mt-6 rounded-2xl bg-slate-50 p-4">
                <p class="text-xs text-slate-600">
                  Note: Pricing is not shown here. You request pricing by selecting an offer and providing exact requirements.
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Offers -->
      <section id="offers" class="mt-12">
        <div class="flex items-end justify-between gap-6">
          <div>
            <h2 class="text-2xl font-bold text-slate-900">Offers</h2>
            <p class="mt-2 text-slate-700">
              Three scope-defined options, similar to Fiverr packages — but written as real deliverables.
            </p>
          </div>
        </div>

        <div class="mt-6 grid gap-6 md:grid-cols-3">
          {offers.map((offer) => (
            <article class="group rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:shadow-md">
              <div class="flex items-start justify-between gap-4">
                <h3 class="text-lg font-bold text-slate-900">{offer.name}</h3>
                <span class="rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-xs font-semibold text-slate-700">
                  {offer.id.toUpperCase()}
                </span>
              </div>

              <div class="mt-5">
                <p class="text-xs font-semibold text-slate-600">Technologies</p>
                <ul class="mt-2 space-y-1 text-sm text-slate-700">
                  {offer.technologies.slice(0, 5).map((t) => (
                    <li class="flex gap-2">
                      <span class="mt-2 h-1.5 w-1.5 rounded-full bg-slate-900"></span>
                      <span>{t}</span>
                    </li>
                  ))}
                </ul>
              </div>

              <div class="mt-5">
                <p class="text-xs font-semibold text-slate-600">Deliverables (summary)</p>
                <ul class="mt-2 space-y-1 text-sm text-slate-700">
                  {offer.deliverables.slice(0, 3).map((d) => (
                    <li class="flex gap-2">
                      <span class="mt-2 h-1.5 w-1.5 rounded-full bg-slate-900"></span>
                      <span>{d}</span>
                    </li>
                  ))}
                </ul>
                {offer.deliverables.length > 3 && (
                  <p class="mt-2 text-xs text-slate-600">Full deliverables are listed on the offer page.</p>
                )}
              </div>

              <div class="mt-6 grid gap-3">
                <a
                  class="rounded-2xl bg-slate-900 px-4 py-3 text-center text-sm font-semibold text-white hover:bg-slate-800"
                  href={`${base}landing/${offer.slug}/`}
                >
                  View offer details
                </a>

                <a
                  class="rounded-2xl border border-slate-300 bg-white px-4 py-3 text-center text-sm font-semibold text-slate-900 hover:bg-slate-100"
                  href={`${base}request-quote?offer=${offer.id}`}
                >
                  Request a quote
                </a>
              </div>
            </article>
          ))}
        </div>
      </section>

      <!-- Process -->
      <section class="mt-12 rounded-3xl border border-slate-200 bg-white p-8">
        <h2 class="text-2xl font-bold text-slate-900">How it works</h2>
        <p class="mt-2 text-slate-700">
          A simple flow designed for clarity (scope first, pricing second).
        </p>

        <div class="mt-6 grid gap-6 md:grid-cols-3">
          <div class="rounded-2xl border border-slate-200 bg-slate-50 p-5">
            <p class="text-xs font-semibold text-slate-600">Step 1</p>
            <p class="mt-2 text-sm font-bold text-slate-900">Choose an offer</p>
            <p class="mt-2 text-sm text-slate-700">Open the offer page and verify included/excluded scope.</p>
          </div>
          <div class="rounded-2xl border border-slate-200 bg-slate-50 p-5">
            <p class="text-xs font-semibold text-slate-600">Step 2</p>
            <p class="mt-2 text-sm font-bold text-slate-900">Describe customizations</p>
            <p class="mt-2 text-sm text-slate-700">Specify what must be changed/added with enough detail to estimate.</p>
          </div>
          <div class="rounded-2xl border border-slate-200 bg-slate-50 p-5">
            <p class="text-xs font-semibold text-slate-600">Step 3</p>
            <p class="mt-2 text-sm font-bold text-slate-900">Submit a pricing request</p>
            <p class="mt-2 text-sm text-slate-700">We receive the request via Web3Forms and respond with next steps.</p>
          </div>
        </div>
      </section>

      <!-- FAQ -->
      <section class="mt-12">
        <h2 class="text-2xl font-bold text-slate-900">FAQ</h2>
        <div class="mt-6 grid gap-4">
          <details class="rounded-2xl border border-slate-200 bg-white p-5">
            <summary class="cursor-pointer text-sm font-semibold text-slate-900">Do you include a backend?</summary>
            <p class="mt-3 text-sm text-slate-700">
              No. This project intentionally stays static. The contact / quote request uses Web3Forms (no backend).
            </p>
          </details>

          <details class="rounded-2xl border border-slate-200 bg-white p-5">
            <summary class="cursor-pointer text-sm font-semibold text-slate-900">Do you promise conversions or traffic?</summary>
            <p class="mt-3 text-sm text-slate-700">
              No. We don&#39;t publish marketing guarantees. Scope is explicit, deliverables are clear.
            </p>
          </details>

          <details class="rounded-2xl border border-slate-200 bg-white p-5">
            <summary class="cursor-pointer text-sm font-semibold text-slate-900">How do I request a custom version?</summary>
            <p class="mt-3 text-sm text-slate-700">
              Use “Request a quote”, select the offer, and describe required customizations and pricing details.
            </p>
          </details>
        </div>
      </section>

      <!-- Footer -->
      <footer class="mt-12 border-t border-slate-200 pt-8 text-sm text-slate-600">
        <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
          <p>© {year} LogicDriveX</p>
          <div class="flex gap-4">
            <a class="hover:text-slate-900" href={`${base}request-quote`}>Request a quote</a>
            <a class="hover:text-slate-900" href="#offers">Offers</a>
          </div>
        </div>
      </footer>

    </div>
  </main>
</SiteLayout>
